# ============================================================
# SEO Content Optimizer — Backend Environment Variables
# Copy this file to .env.local and fill in your values.
# ============================================================

# ── MongoDB ──────────────────────────────────────────────────
# Production database
MONGODB_URI=mongodb+srv://<user>:<password>@cluster.mongodb.net/seo-tracker

# Test database
MONGODB_URI_TEST=mongodb+srv://<user>:<password>@cluster.mongodb.net/seo-tracker-test

# ── Server ────────────────────────────────────────────────────
NODE_ENV=development

# ── Optional: Rate limiting ───────────────────────────────────
RATE_LIMIT_REQUESTS=10
RATE_LIMIT_WINDOW_MS=60000
